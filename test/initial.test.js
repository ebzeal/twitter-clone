/* eslint-disable no-undef */
describe('Test setup', () => {
  test('Jest implementation', () => {
    const actual = ['me', 'you', 'them', 'us'];
    expect(actual).toEqual(['me', 'you', 'them', 'us']);
  });
});
